# 08 - 直充余额美元标识与订阅标签对比度

**Type:** task  
**Status:** resolved

## 范围

- 将 `Grok 直充余额` 的原生 capture、预览 fixture 与显示统一为美元 `$` / `USD`。
- 保持 Graphite Rail，令顶部当前订阅名称使用与当前尺寸主 UI 相称的字号，并以深色底衬提高对比度。

## 非范围

- 不改登录、认证读取范围、五分钟刷新、订阅切换、贴边隐藏、托盘或设置业务逻辑。

## 验收

- `Grok 直充余额` 显示为例如 `$298.69`，不显示 `¥`。
- 大、中、小尺寸的顶部订阅名称均清晰、截断安全，并有深色底衬。
- 单元测试、前端构建和 Rust 检查通过。

## Answer

- 原生 `/auth/me` 余额 capture 现在规范化为 `$` / `USD`；未扩大读取字段或持久化范围。
- 预览、解析器和组件测试已同步，防止 `¥` 标识回归。
- 顶部订阅名增加石墨深色底衬、白色粗体和按大/中/小尺寸收缩的字号，保留原有分页按钮与拖拽热区。
- 通过前端 49 项测试、Rust 5 项测试、前端生产构建、Rust check 和 Windows MSI/NSIS 生产打包；240px 紧凑预览无页面溢出。
