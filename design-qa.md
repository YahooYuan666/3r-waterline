# 贴边刻度视觉 QA

## 比较目标

- 视觉来源：火绒侧栏两排进度（周/月各 10 个点）。亮格是余量，暗格是消耗。
- 目标状态：14px 贴边把手；每排固定 10 点；点高 2px、点距 2px（等高等距）、组距 8px；点宽 10px。侧边横向细线纵向堆叠并居中成簇，余量在下、消耗从上往下长；上下边竖向细线横向排列，余量在左、消耗从右往左。
- 实现：只用一块 canvas 按设备像素整数画 2px 刻度（线宽=间距=`round(2×dpr)`）。`.edge-meter-dot` 只给测试计数，不参与绘制。不用 1px 重复渐变，也不用 10 个独立 CSS 盒子当视觉源。

## 已核对

- 字体与文案：该控件无可见文字。
- 间距与布局：组内 2px 线 + 2px 空隙等高度；周/月组间 8px。
- 颜色：emerald 周点、cobalt 月点；已消耗点为同色低对比。
- 方向：亮=余量，暗=消耗；侧边消耗从上到下，横板消耗从右到左。
- 响应性：把手仍为 14px，刻度不随窗口高度拉伸。

## Findings

- 根因不是 2px 这个尺寸错了：10 个独立 CSS 盒子在 125%/150% 下会各自取整，一格 2、一格 3。改成 1px 渐变更密，更容易糊成一条。
- 125% 下 2 CSS 像素 = 2.5 设备像素，无法画成等高。canvas 用 `round(2×dpr)` 个设备像素，显示略大于 2px，但每格等高。
- 最终观感应在新便携包上贴边确认。

final result: pass
