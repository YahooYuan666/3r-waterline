# 贴边刻度视觉 QA

## 比较目标

- 视觉来源：火绒侧边栏两排进度（周/月各 10 个点）。
- 目标状态：14px 贴边把手；每排固定 10 点；点高 2px、点距 2px、组距 8px；点宽 10px。侧边横向细线纵向堆叠并居中成簇；上下边竖向细线横向排列。
- 实现：`EdgeHideMeter` 绘制 10 个 `.edge-meter-dot`，用 CSS grid 锁死点高和间距，不再用渐变铺满窗口。

## 已核对

- 字体与文案：该控件无可见文字。
- 间距与布局：组内 2px 线 + 2px 空隙均匀分布；周/月组间 8px。
- 颜色：emerald 周点、cobalt 月点；未消耗点为同色低对比。
- 响应性：把手仍为 14px，刻度不随窗口高度拉伸。

## Findings

- 旧 1px 点在缩放下会糊掉、间距不匀；铺满渐变已撤回。
- 最终观感应在新便携包上贴边确认。

final result: pass
