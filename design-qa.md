# 贴边刻度视觉 QA

## 比较目标

- 视觉来源：火绒侧边栏密线效果（周/月两组细线铺满把手，组间留白）。
- 目标状态：14px 贴边把手；侧边为横向 1px 细线纵向堆叠，上下边为竖向 1px 细线横向排列。
- 实现：`EdgeHideMeter` 用 `--remaining` 裁切密线填充，不再绘制 10 个独立点。

## 已核对

- 字体与文案：该控件无可见文字。
- 间距与布局：组内 1px 线 + 1px 空隙，周/月组间 8px。
- 颜色：emerald 周填充、cobalt 月填充；未消耗区为低对比灰线。
- 响应性：把手仍为 14px，不扩大贴边窗口。

## Findings

- 代码结构已对齐火绒密线；最终观感应在新便携包上现场确认。

final result: pass
