# 复核报告：直充余额美元标识与订阅标签

日期：2026-08-25

## 复核范围

- 对照 `.scratch/3r-waterline/issues/08-direct-balance-usd-and-subscription-label.md` 复核实现与构建结果。

## 结果

- 通过：余额的原生格式化、前端 fixture 与断言均使用 `$` / `USD`；通用 `¥` 货币解析仍保留给未来普通订阅，不会把其误判为美元。
- 通过：订阅标题样式只作用于 `.subscription-header` 与其 `strong`，未改动导航按钮的事件、拖拽目标或额度展示结构。
- 通过：`git diff --check`、前端单测、前端构建、Rust 格式/测试/check 与 Windows 生产打包均成功。
- 通过：紧凑 240px 渲染中，标题的深色底衬为 `rgba(7, 12, 18, 0.82)`，标题和余额文本没有横向或纵向页面溢出。

## 风险

- 未在用户当前正在运行的安装版上做替换安装；用户可退出旧版本后安装本次生成包进行最终现场确认。

## 2026-08-25 补充：贴边刻度节奏

- 复核通过：`.edge-hide-meter` 与顶部/底部覆盖均为 8px 组间距；`.edge-meter-track` 与顶部/底部覆盖均为 2px 组内间距。
- 复核通过：React 结构和每组 10 段的计算未改变，现有组件测试仍验证双周期为 20 个刻度。
- 限制：无法在不干扰用户正在运行的单实例程序的情况下，捕捉原生贴边状态；需要新包的现场目测确认。
