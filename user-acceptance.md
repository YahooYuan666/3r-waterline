# 用户验收：余额美元标识与订阅标签

1. 退出正在运行的旧版 3R Waterline，再安装或运行本次 Windows 构建产物。
2. 在悬浮窗顶部使用左右按钮切换到 `Grok 直充余额`：金额应显示 `$`，不应显示 `¥`，且不应出现分母或重置时间。
3. 切换回任一正常订阅：顶部订阅名称应为白色粗体、置于深色标签底衬中；周/月两条额度仍正常显示。
4. 在设置中切换大、中、小尺寸，以及水瓶/Traffic Monitor：订阅名称应保持清晰，不遮挡左右切换按钮。
